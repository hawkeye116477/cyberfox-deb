#!/bin/sh
set -e

. /usr/share/debconf/confmodule

available()
{
        command -v "$1" >/dev/null 2>&1
}

case "$1" in
    purge)
        rm -f /etc/apt/sources.list.d/cyberfox-release.list
        rm -f /etc/update-manager/release-upgrades.d/cyberfox-release.cfg
        ;;
esac

exit 0
